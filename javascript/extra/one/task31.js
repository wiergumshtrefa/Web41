const prompt = require('prompt-sync')();
let x = Number(prompt("введите значение х:"));
let y = Number(prompt("введите значение y:"));

console.log(` а) ( x + y ) / 2 = ${( x + y ) / 2}`)
console.log(` б) ( x * y ) / 2 = ${( x * y ) / 2}`)